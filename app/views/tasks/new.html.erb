<h2>Add a new Task</h2>
<%= form_for(@task) do |t| %>
  <%= t.text_field :title %>
  <%= t.text_field :details %>
  <%= t.submit %>
<% end %>
<p><%= link_to "Back to list", tasks_path('/index')  %></p>
